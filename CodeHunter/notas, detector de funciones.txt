
herramienta detector de funciones


para empezar hay que buscar en power shell o cmd
la direccion de las carpetas, en mi caso:

borrar venv

rmdir .venv -Recurse -Force
python -m venv .venv

python -m pip install --upgrade pip     --Actualizar pip
pip install rich click pylint           --Instalar dependencias del proyecto
pip freeze > requirements.txt           --crear requirements.txt
pip install -r requirements.txt         --Guardar dependencias

.\.venv\Scripts\Activate.ps1            --Activar el venv

ls .venv                                --Verifica la estructura
ls .venv\Scripts

python -m venv .venv                    --crea el entorno

.venv\Scripts\Activate.ps1              -- activar el venv

pip install -r requirements.txt        -- para instalar en otro pc


librerias:

pip install pandas

python -m CodeHunter             /para ejecutar CodeHunter

python -m CodeHunter.main

python main_gui.py             ejecutar la gui


C:\Users\ADMIN\Downloads\CodeHunter_Analizer -0.2


git status      si se modifica algo


git add .
git commit -m     "Agregué nueva función de análisis"
git push

git diff    ver que cambio
 
git log               ver historial
git log --oneline    veer historial resumida

git grep "nombre_de_funcion"    buscar palabra en proyecto

git restore nombre_archivo.py        atras  si algo sale mal
git checkout ID_DEL_COMMIT            volver a un commit

git diff       comparar cambion entre commit

git show d33cb5b         ver que cambio en un commit

 git pull     (para traer lo último)    

git clone https://github.com/Jeremy2730/Code-Hunter-Analizer.git   clonarlo en otra pc

Code-Hunter-Analizer        sale esta carpeta

cd Code-Hunter-Analizer    luego se entra aqui
